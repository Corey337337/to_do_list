<Window x:Class="to_do_list.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:to_do_list"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="400">
    <Canvas>
        <ListView x:Name="MusicListView" Height="399" Width="400" HorizontalAlignment="Center" VerticalAlignment="Top" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Title}" FontSize="50" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Artist}" FontSize="30" />
                        <TextBlock Text="{Binding Album}" FontSize="30"/>
                        <TextBlock Text="{Binding Year}" FontSize="30"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>

        <Button x:Name="Add" Content="Add Music" Canvas.Left="121" Canvas.Top="470"  Width="150" Height="30" Click="Add_Click" HorizontalAlignment="Left" VerticalAlignment="Top" />


    </Canvas>
</Window>
